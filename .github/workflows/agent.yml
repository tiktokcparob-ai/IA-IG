name: 🤖 KODA-7 AI Agent

on:
  workflow_dispatch:
    inputs:
      mode:
        description: 'وضع التشغيل'
        required: true
        default: 'agent'
        type: choice
        options:
          - agent
          - scheduler
          - maintenance
  schedule:
    - cron: '0 */6 * * *'  # كل 6 ساعات

jobs:
  koda-agent:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    permissions:
      contents: write
      issues: write
      pull-requests: write

    steps:
      # ═══════════════════════════════════════
      # 1️⃣ Checkout (يحتاج token هنا!)
      # ═══════════════════════════════════════
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GH_PAT }}  # ← السر المنفصل المطلوب

      # ═══════════════════════════════════════
      # 2️⃣ Python
      # ═══════════════════════════════════════
      - name: 🐍 Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      # ═══════════════════════════════════════
      # 3️⃣ Dependencies
      # ═══════════════════════════════════════
      - name: 📦 Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyTelegramBotAPI groq PyGithub python-dotenv requests rich schedule

      # ═══════════════════════════════════════
      # 4️⃣ .env من السر المجمع
      # ═══════════════════════════════════════
      - name: 🔐 Setup Environment
        run: |
          echo "${{ secrets.ALL_SECRETS }}" > .env
          echo "RUN_ID=${{ github.run_id }}" >> .env
          echo "RUN_NUMBER=${{ github.run_number }}" >> .env
          echo "REPO=${{ github.repository }}" >> .env

      # ═══════════════════════════════════════
      # 5️⃣ تشغيل الوكيل
      # ═══════════════════════════════════════
      - name: 🤖 Run KODA-7 Agent
        run: python agent.py --mode ${{ github.event.inputs.mode || 'agent' }}
        env:
          PYTHONUNBUFFERED: 1

      # ═══════════════════════════════════════
      # 6️⃣ Commit تلقائي (اختياري)
      # ═══════════════════════════════════════
      - name: 💾 Auto Commit Changes
        run: |
          git config --local user.email "koda7@bot.ai"
          git config --local user.name "KODA-7 AI"
          git add -A
          git diff --quiet && git diff --staged --quiet || git commit -m "🤖 KODA-7 auto-update [run ${{ github.run_number }}]"
          git push

      # ═══════════════════════════════════════
      # 7️⃣ تنظيف
      # ═══════════════════════════════════════
      - name: 🧹 Cleanup
        if: always()
        run: rm -f .env
        
